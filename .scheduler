#!/bin/bash

# Run this script daily and it will route to the other tasks

MY_PATH=`dirname "$0"`
echo Checking scheduler

# Scrape financial data every 15th of the month:
if [ "$(date +%-d)" = 15 ]; then
   npm start && curl -L http://pshmn.com/WqFn2Y
fi

echo Finished checking


# Examples:
# Run a task every Monday:
# if [ "$(date +%u)" = 1 ]; then
#    MY_COMMAND
# fi

# Run a task every 1st day in a month:
# if [ "$(date +%-d)" = 1 ]; then
#    MY_COMMAND
# fi

# Run a task every 3 days:
# if [ "$(( $(date +%-d) % 3 ))" = 1 ]; then
#    MY_COMMAND
# fi

# Run a task every year on December 9th:
# if [ "$(date +%m)" = 12 ] && [ "$(date +%d)" = 09 ]; then
#    MY_COMMAND
# fi